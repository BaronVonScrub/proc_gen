(
    structure_name: "Large Branch",
    tags: ["Tree"],
    data: [
        // Central trunk/branch
        (Nest(Ref(
            structure: "Trees/branch",
            ownership: Team(1),
        )), (
            translation: (0.0, 0.0, 0.0),
            rotation: (0.0, 0.0, 0.0),
            scale: (1.0, 1.0, 1.0),
        )),

        // Three branch clusters spaced along the length (Y axis)
        (LoopParam(
            reference: Ref(
                structure: "Trees/branch_cluster",
                ownership: Team(1),
            ),
            origin: (0.0, 1.5, 0.0),      // first cluster position on trunk
            rotation: (0.0, 0.0, 0.0),    // no per-index yaw/pitch/roll
            distance: 0.0,                // place at origin; spacing handled by child_position
            child_position: (0.0, 1.5, 0.0), // move +2.0 on Y per index -> y = 2, 4, 6
            child_rotation: (0.0, 0.0, 0.0), // no additional per-index rotation
            child_scale: (-0.2, -0.2, -0.2),    // no per-index scaling
            count: 3,
        ), (
              translation: (0.0, 0.0, 0.0),
            rotation: (0.0, 0.0, 0.0),
            scale: (1.0, 1.0, 1.0),
        )),
    ],
)
